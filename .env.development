# Development API target
# Point frontend directly to local FastAPI (bypasses Vite proxy/port confusion)
VITE_API_URL=http://127.0.0.1:8082

# Local development identity used only when the backend is in header auth mode.
# Do not rely on browser-provided identity for production.
VITE_SENTRY_USER=j0w16ja
