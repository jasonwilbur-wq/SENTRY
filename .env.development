# Development — empty string = Vite proxy forwards /api/* to FastAPI on :8082
VITE_API_URL=

# User identity for header-based auth (Phase 1).
# Must match a user in SENTRY_ALLOWED_USERS on the backend.
# Leave empty only if backend is running with SENTRY_AUTH_MODE=off.
VITE_SENTRY_USER=j0w16ja
